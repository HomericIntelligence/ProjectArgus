run:
  timeout: 5m
  modules-download-mode: readonly

linters:
  enable:
    - govet
    - staticcheck
    - errcheck
    - ineffassign
    - unused
    - gosec

issues:
  exclude-rules:
    - linters: [gosec]
      text: "G401|G501"
